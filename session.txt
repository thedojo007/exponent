Jarvis: Agent Spec

New standing step in the end-session protocol (not a ClickUp Brain agent — a Claude session-partner behavior): at every "end session," before the recap, scan for anything resolved during the session — shipped or decided-without-shipping — and check it against Build Log for a matching task before closing out. Demonstrated manually twice today; now a standing step, not opportunistic. This is the "Closure path" half of the Session-to-Task pipeline rescope below — deliberately kept manual, not scripted, until it's run across enough real sessions to know what a matcher would need to handle.

Jarvis: ClickUp Setup Notes

Workspace audit, cross-referenced against What Works v5 (145 rows, up from the 129 the last System Board snapshot cites):

Build Log (95 tasks): 68% backlog, 18% shipped, 9% cancelled. Priority null on all but 4; due date null on effectively all. No staleness/downgrade mechanism exists for Build Log the way task_easer.py covers Task Easer Machine.
Components (31 tasks): 84% shipped — accurate, functions as real source of truth for what's live.
Outstanding List (Kumyang work): near-100% completion, fast cycle time — contrast case proving execution is strong under real stakes.
Daily Log (27 entries): status field never used — 100% "to do," 0% ever "complete." All signal lives in free text, not queryable. Two silent 5-day gaps found with no entries and no flag (Aug 1–5, Aug 7–11).

Jarvis: Build Log

8/24: Decided (not shipped): task_easer.py 'eased' tag — manual-only reset confirmed, no auto-clear logic existed to remove. Task 86e2gm9gh → cancelled.
8/24: Decided (not shipped): 학습 규칙 주간 요약 scope-drift task — root cause was structural (cloud-scheduled task hitting a permanent local-reach wall), not a scope bug. Superseded by the already-shipped kumyoung-cash-report-learn skill. Task 86e2y2tq8 → cancelled.
8/24: Rescoped: Session-to-Task pipeline (86e2y2tp8) — split into Creation path (deferred) and Closure path (live now as process, not code). Status → scoping.

Jarvis: Graveyard Log

8/24: Graveyard: Weekly SKILL.md diff-tracking (학습 규칙 scope drift task, Step 1) — never surfaced a real finding across its runs; killed via negativa, not rebuilt local.

Jarvis State File

As of 8/24: task_easer.py eased-tag policy closed (manual-only, no code). 학습 규칙 주간 요약 weekly task closed/superseded by kumyoung-cash-report-learn skill; its SKILL.md-diff half killed outright. Session-to-Task pipeline rescoped — Closure path live as process, Creation path deferred pending real-run data. Open: Graveyard Log ClickUp doc still has no confirmed doc ID — Graveyard entries have nowhere to land automatically.
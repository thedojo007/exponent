Jarvis: Agent Spec

New standing constraint discovered, applies to all future device-dependent builds: cloud-scheduled Claude tasks cannot reach local MCP bridges (remote-devices) — confirmed structurally, not intermittently. Any automation needing local file/device access (ERP receipt automation, future device-touching builds) must be either interactive or a Python script + Windows Task Scheduler job, never a Claude scheduled task.
자금일보 학습 규칙 업데이트: converted from a ClickUp-scheduled cloud task to a Claude Desktop Skill (스킬로 전환), invoked manually going forward — cloud trigger was confirmed dead by design, not a bug.
Claude now has direct ClickUp connector read/write access in chat (confirmed live: clickup_create_task, clickup_search, clickup_filter_tasks, doc CRUD, etc.). Standing rule added: writes require explicit go-ahead each time; jarvis_session_sync.py stays default write path for Build Log/State File. Read-only calls don't require confirmation.

Jarvis: ClickUp Setup Notes

Outstanding List confirmed at list ID 901715544578 under Professional Outcomes space (id 90173603574), 21 open items surveyed. All 21 have priority: null; 20/21 have no due date. Offer still open to set real values on these.

Jarvis: Build Log

8/21: Shipped: daily_log.py — added Outstanding List leverage-picks module (2 lowest-input/highest-Claude-output tasks, hard-excludes financial authorization items), placed at top of Page 1 above the task table; confirmed working end-to-end.

git add .
git commit -m "Add leverage-picks module to daily_log.py"
git push

Jarvis: Graveyard Log

8/21: Graveyard: leverage-picks module's first home in jarvis_next_steps.py — wrong artifact (writes to a ClickUp doc you don't physically review each morning); superseded by placement in daily_log.py, the file actually printed/read daily.
8/21: Graveyard: 자금일보 학습 규칙 업데이트 scheduled task auto-trigger — remote-devices connector not reachable from cloud-scheduled sessions, structurally, regardless of device sleep/wake state or manual retry.

Jarvis State File

Shipped: daily_log.py leverage-picks module.
Open/unresolved: (1) 학습 규칙 주간 요약 weekly task showing scope drift — reported 4 skills instead of the scoped 1, meaning ~/skill_rules_watch/repo's git history isn't persisting across weekly cloud runs. This is a shipped-but-misbehaving item, not a new build — worth the 2:1 loss-aversion framing if it stings on review: fix the persistence/scope bug, don't scrap the task. (2) D:\2.3 자금 일보 자료 folder access via Claude Desktop's "Add folder" — requested, not yet confirmed granted or re-tested end-to-end.
Deferred: setting real priority/due-date values on the 21 open Outstanding List items — offered twice, not yet actioned.